# Configuration file for local development
policyDefinitions:
  cors:
    origin: "*"

endpoints:
  - path: "/example"
    target:
      url: "https://httpbin.org/bearer"
    request:
      headers:
        Authorization: "Bearer ${AUTH_TOKEN}"
    policies:
      - cors
